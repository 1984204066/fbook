#+macro: html-audio-tabs (eval (progn (setq embed-str "<embed src=\"%s\" width=\"530px\" height=\"80px\" />") (setq ahref-str "<a href=\"%s\">%s</a>") (setq edited_audio (if (not (string-lessp "" $1)) "" (format embed-str (concat "media/edited/" $1)))) (setq raw_audio (if (not (string-lessp "" $2)) "" (format embed-str (concat "media/raw/" $2)))) (setq docx_format (if (not (string-lessp "" $3)) "" (format ahref-str (concat "./docx/" $3) "docx"))) (setq pdf_format (if (not (string-lessp "" $4)) "" (format ahref-str (concat "./pdf/" $4) "pdf"))) (shell-command-to-string (format "sed -e 's,edited_audio,%s,' -e 's,raw_audio,%s,' -e 's,docx_format,%s,' -e 's,pdf_format,%s,' -e 's,speak_date,%s,' template" edited_audio raw_audio docx_format pdf_format $5))))
